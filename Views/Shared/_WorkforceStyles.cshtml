<style>
    .workforce-page {
        --workforce-bg: #ffffff;
        --workforce-ink: #0a0a0a;
        --workforce-muted: #888888;
        --workforce-border: #e8e8e8;
        --workforce-soft: #f8f8f8;
        font-family: 'DM Sans', sans-serif;
        color: var(--workforce-ink);
    }

    .workforce-page .workforce-title,
    .workforce-page .workforce-form-header h4 {
        margin: 0;
        font-family: 'DM Sans', sans-serif;
        font-size: 22px;
        line-height: 1.1;
        font-weight: 600;
        color: var(--workforce-ink);
    }

    .workforce-page .workforce-sub,
    .workforce-page .small.text-muted {
        font-size: 14px;
        line-height: 1.6;
        color: #606060 !important;
        font-weight: 400;
    }

    .workforce-page .form-label,
    .workforce-page .workforce-table thead th,
    .workforce-page .workforce-status,
    .workforce-page .admin-section-kicker,
    .workforce-page .form-floating > label {
        font-family: 'DM Mono', monospace;
        font-size: 9px;
        letter-spacing: 0.14em;
        text-transform: uppercase;
        color: var(--workforce-muted);
    }

    .workforce-header,
    .workforce-filter,
    .workforce-card,
    .workforce-form-card {
        background: var(--workforce-bg);
        border: 1px solid var(--workforce-border);
        border-radius: var(--ui-radius-xl);
        box-shadow: none;
    }

    .workforce-header {
        display: flex;
        flex-wrap: wrap;
        justify-content: space-between;
        align-items: center;
        gap: 16px;
        padding: 28px 32px;
        margin-bottom: 20px;
    }

    .workforce-btn,
    .workforce-page .btn {
        border-radius: var(--ui-radius-md);
        box-shadow: none !important;
        font-size: 14px;
        font-weight: 500;
        padding: 10px 16px;
    }

    .workforce-btn-neutral,
    .workforce-page .btn-outline-danger,
    .workforce-page .btn-outline-success {
        background: #ffffff;
        border: 1px solid var(--workforce-border);
        color: var(--workforce-ink);
    }

    .workforce-btn-primary {
        background: #0a0a0a;
        border: 1px solid #0a0a0a;
        color: #ffffff;
    }

    .workforce-btn-neutral:hover,
    .workforce-page .btn-outline-danger:hover,
    .workforce-page .btn-outline-success:hover {
        background: var(--workforce-soft);
        color: var(--workforce-ink);
        border-color: #dcdcdc;
    }

    .workforce-btn-primary:hover {
        background: #000000;
        border-color: #000000;
        color: #ffffff;
    }

    .workforce-filter {
        display: flex;
        flex-wrap: wrap;
        gap: 14px;
        align-items: end;
        padding: 18px 20px;
        margin-bottom: 16px;
    }

    .workforce-form-shell {
        max-width: 860px;
        margin: 0 auto;
    }

    .workforce-form-header {
        padding: 24px 28px;
        border-bottom: 1px solid var(--workforce-border);
        background: #ffffff;
    }

    .workforce-form-body {
        padding: 24px 28px;
        background: #ffffff;
    }

    .workforce-page .form-control,
    .workforce-page .form-select {
        min-height: 44px;
        border: 1px solid transparent;
        border-bottom-color: var(--workforce-border);
        border-radius: var(--ui-radius-md);
        background: #ffffff;
        box-shadow: none;
        color: var(--workforce-ink);
        font-size: 15px;
        font-weight: 400;
        padding-left: 0;
        padding-right: 0;
    }

    .workforce-page .form-floating > .form-control,
    .workforce-page .form-floating > .form-select {
        padding-left: 0;
        padding-right: 0;
    }

    .workforce-page .form-floating > label {
        padding-left: 0;
        padding-right: 0;
    }

    .workforce-page .form-control:focus,
    .workforce-page .form-select:focus {
        border-color: transparent;
        border-bottom-color: #0a0a0a;
        box-shadow: none;
    }

    .workforce-page .form-check-input {
        border-radius: 999px;
        border-color: #cfcfcf;
        box-shadow: none;
    }

    .workforce-page .form-check-input:checked {
        background-color: #0a0a0a;
        border-color: #0a0a0a;
    }

    .workforce-table {
        margin-bottom: 0;
    }

    .workforce-table thead th {
        background: #ffffff;
        border-bottom: 1px solid var(--workforce-border);
        padding: 14px 16px;
        font-weight: 400;
    }

    .workforce-table td {
        padding: 16px;
        vertical-align: middle;
        border-color: var(--workforce-border);
        font-size: 14px;
    }

    .workforce-table tbody tr:hover {
        background: #fbfbfb;
    }

    .workforce-money {
        font-family: 'DM Sans', sans-serif;
        font-weight: 500;
        white-space: nowrap;
        color: var(--workforce-ink);
    }

    .workforce-status {
        display: inline-flex;
        align-items: center;
        justify-content: center;
        padding: 5px 10px;
        border-radius: 999px;
        border: 1px solid var(--workforce-border);
        background: #ffffff;
        color: var(--workforce-ink);
    }

    .workforce-status-active,
    .workforce-status-archived,
    .workforce-status-locked,
    .workforce-status-open {
        background: #ffffff;
        color: inherit;
    }

    .workforce-card .card-footer {
        background: #ffffff;
        border-top: 1px solid var(--workforce-border);
        padding: 18px 20px;
    }

    @@media (max-width: 768px) {
        .workforce-header,
        .workforce-filter,
        .workforce-form-header,
        .workforce-form-body {
            padding: 20px;
        }
    }

    .workforce-header,
    .workforce-filter,
    .workforce-card,
    .workforce-form-card {
        border-radius: var(--ui-radius-xl) !important;
    }

    .workforce-btn,
    .workforce-page .btn,
    .workforce-page .form-control,
    .workforce-page .form-select {
        border-radius: var(--ui-radius-md) !important;
    }
</style>
