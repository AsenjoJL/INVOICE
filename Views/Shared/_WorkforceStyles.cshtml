<style>
    .workforce-page {
        font-family: 'DM Sans', sans-serif;
        color: #0f172a;
    }

    .workforce-header {
        display: flex;
        flex-wrap: wrap;
        justify-content: space-between;
        align-items: center;
        gap: 0.75rem;
        background: #ffffff;
        border: 2px solid #cbd5e1;
        border-radius: 0;
        box-shadow: none;
        padding: 1rem 1.2rem;
        margin-bottom: 1rem;
    }
    .workforce-title {
        margin: 0;
        font-family: 'Poppins', sans-serif;
        font-weight: 800;
        color: #000000;
    }
    .workforce-sub {
        margin: 0.22rem 0 0;
        color: #334155;
        font-weight: 600;
        font-size: 0.92rem;
    }

    .workforce-btn {
        border-radius: 0;
        font-weight: 700;
        box-shadow: none !important;
    }
    .workforce-btn-neutral {
        border: 1px solid #cbd5e1;
        background: #ffffff;
        color: #0f172a;
    }
    .workforce-btn-primary {
        border: 1px solid #0f766e;
        background: #0f766e;
        color: #ffffff;
    }

    .workforce-filter {
        display: flex;
        flex-wrap: wrap;
        gap: 0.5rem;
        align-items: end;
        background: #ffffff;
        border: 1px solid #e2e8f0;
        border-radius: 0;
        padding: 0.7rem 0.8rem;
        margin-bottom: 0.9rem;
    }
    .workforce-filter .form-control,
    .workforce-filter .form-select {
        border-radius: 0;
        border: 1px solid #cbd5e1;
        font-weight: 600;
        box-shadow: none;
    }

    .workforce-card {
        border: 2px solid #cbd5e1;
        border-radius: 0;
        box-shadow: none;
        overflow: hidden;
        background: #ffffff;
    }

    .workforce-table {
        margin-bottom: 0;
    }
    .workforce-table thead th {
        background: #f1f5f9;
        color: #334155;
        text-transform: uppercase;
        letter-spacing: 0.05em;
        font-size: 0.75rem;
        font-weight: 700;
    }
    .workforce-table tbody tr:nth-child(even) {
        background: #fafcff;
    }
    .workforce-table tbody tr:hover {
        background: #f0fdfa;
    }
    .workforce-money {
        font-family: Consolas, "Courier New", monospace;
        font-weight: 700;
        white-space: nowrap;
    }
    .workforce-status {
        display: inline-flex;
        align-items: center;
        border-radius: 999px;
        padding: 0.26rem 0.7rem;
        font-size: 0.74rem;
        font-weight: 700;
        text-transform: uppercase;
        letter-spacing: 0.03em;
    }
    .workforce-status-active {
        color: #166534;
        background: #dcfce7;
    }
    .workforce-status-archived {
        color: #475569;
        background: #e2e8f0;
    }
    .workforce-status-locked {
        color: #475569;
        background: #e2e8f0;
    }
    .workforce-status-open {
        color: #0e7490;
        background: #cffafe;
    }

    .workforce-form-shell {
        max-width: 760px;
        margin: 0 auto;
    }
    .workforce-form-card {
        border: 2px solid #cbd5e1;
        border-radius: 0;
        box-shadow: none;
        overflow: hidden;
        background: #ffffff;
    }
    .workforce-form-header {
        padding: 0.9rem 1rem;
        border-bottom: 1px solid #e2e8f0;
        background: #ffffff;
    }
    .workforce-form-header h4 {
        margin: 0;
        font-family: 'Poppins', sans-serif;
        font-weight: 800;
        color: #000000;
    }
    .workforce-form-body {
        padding: 1rem;
    }
    .workforce-form .form-control,
    .workforce-form .form-select,
    .workforce-form .form-check-input {
        border-radius: 0;
        border: 1px solid #cbd5e1;
        box-shadow: none;
    }
    .workforce-form .form-control,
    .workforce-form .form-select {
        font-weight: 600;
    }
    .workforce-form .form-control:focus,
    .workforce-form .form-select:focus {
        border-color: #0f766e;
        box-shadow: 0 0 0 3px rgba(15, 118, 110, 0.2);
    }
</style>
