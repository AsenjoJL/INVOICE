<style>
    @@import url('https://fonts.googleapis.com/css2?family=Manrope:wght@400;500;600;700&display=swap');

    .product-form-page {
        font-family: 'Manrope', 'Segoe UI', sans-serif;
        color: #0f172a;
    }
    .product-form-shell {
        max-width: 760px;
        margin: 0 auto;
    }
    .product-form-card {
        background: #fff;
        border-radius: 24px;
        border: 1px solid rgba(148, 163, 184, 0.25);
        overflow: hidden;
        box-shadow: 0 24px 48px rgba(15, 23, 42, 0.14);
    }
    .product-form-banner {
        background: linear-gradient(135deg, #1d4ed8 0%, #0ea5e9 55%, #14b8a6 100%);
        color: #f8fafc;
        padding: 22px 28px;
    }
    .product-form-kicker {
        font-size: 0.75rem;
        text-transform: uppercase;
        letter-spacing: 0.15em;
        font-weight: 600;
        opacity: 0.8;
    }
    .product-form-banner h4 {
        margin: 4px 0 6px;
        font-weight: 700;
        font-size: 1.5rem;
    }
    .product-form-banner .subtitle {
        margin: 0;
        opacity: 0.9;
        font-size: 0.95rem;
    }
    .product-form-banner .btn {
        border-radius: 999px;
        font-weight: 600;
        padding: 6px 14px;
    }
    .product-form-body {
        background: linear-gradient(180deg, #f8fafc 0%, #eef2f7 100%);
        padding: 22px 26px 26px;
    }
    .product-form-alert {
        margin-bottom: 16px;
    }
    .product-form-section {
        background: #fff;
        border-radius: 18px;
        padding: 18px 20px;
        border: 1px solid rgba(226, 232, 240, 0.9);
        box-shadow: 0 10px 24px rgba(15, 23, 42, 0.08);
        margin-bottom: 16px;
    }
    .product-form-section:last-child {
        margin-bottom: 0;
    }
    .section-title {
        font-size: 0.8rem;
        text-transform: uppercase;
        letter-spacing: 0.12em;
        font-weight: 700;
        color: #64748b;
        margin-bottom: 12px;
    }
    .product-form-card .form-control,
    .product-form-card .form-select {
        border-radius: 14px;
        border-color: #e2e8f0;
        background: #f8fafc;
        color: #0f172a;
    }
    .product-form-card .form-control:focus,
    .product-form-card .form-select:focus {
        border-color: #38bdf8;
        box-shadow: 0 0 0 3px rgba(56, 189, 248, 0.25);
    }
    .product-form-card .form-floating > label {
        color: #64748b;
    }
    .sku-pill {
        display: inline-flex;
        align-items: center;
        gap: 6px;
        background: #e0f2fe;
        color: #0369a1;
        border-radius: 999px;
        padding: 6px 12px;
        font-size: 0.72rem;
        font-weight: 700;
        text-transform: uppercase;
        letter-spacing: 0.08em;
    }
    .sku-help {
        font-size: 0.8rem;
        color: #64748b;
        margin-top: 6px;
    }
    .product-form-actions {
        display: flex;
        justify-content: space-between;
        gap: 12px;
        flex-wrap: wrap;
    }
    .product-form-actions .btn {
        border-radius: 999px;
        font-weight: 600;
        padding: 10px 18px;
    }
    .product-form-actions .btn-primary {
        background: linear-gradient(135deg, #2563eb, #0ea5e9);
        border: none;
        box-shadow: 0 12px 24px rgba(37, 99, 235, 0.25);
    }
    .product-form-actions .btn-outline-secondary {
        border-color: #cbd5f5;
        color: #1e293b;
    }
    .product-form-actions .btn-outline-secondary:hover {
        background: #e2e8f0;
    }
    .form-check-input:checked {
        background-color: #22c55e;
        border-color: #22c55e;
    }

    @@media (max-width: 768px) {
        .product-form-body {
            padding: 18px;
        }
        .product-form-banner {
            padding: 18px 20px;
        }
    }
</style>
